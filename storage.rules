rules_version = '2';

// Comprovantes are uploaded/read via firebase-admin after session checks.
// Client SDK access is denied.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
